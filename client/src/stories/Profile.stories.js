import React from "react";

import { action } from "@storybook/addon-actions";
import ProfileImage from "../profile/ProfileImage";

export default {
  title: "Profile Image",
  component: ProfileImage,
};

export const Default = () => <ProfileImage />;
